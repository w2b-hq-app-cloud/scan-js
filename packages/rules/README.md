# @spherescan/rules

Connection legality for SCAN - which element kinds may connect and with which port roles.

```bash
npm install @spherescan/rules @spherescan/model
```

Used by the modeler and CLI validation path. See `skills/scan-notation/connection-rules.md` for the human-readable matrix.

License: Apache-2.0.
