# Architecture examples

Sample SCAN diagrams (`.scan.yaml`) for demos and tests.

| File | Description |
|------|-------------|
| `order-platform.scan.yaml` | Multi-kind commerce board (canonical fixture) |
| `hello-scan.scan.yaml` | Minimal two-node starter |

Validate:

```bash
node ../../packages/cli/dist/cli.js validate order-platform.scan.yaml
```
